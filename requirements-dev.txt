# Dev/test-only dependencies. Installed in CI and local test runs, never in production.
# Keep production deps in requirements.txt clean.
#
# Note: httpx (needed by FastAPI's TestClient for Phase 3) is already pinned in
# requirements.txt, so it isn't repeated here.

pytest>=8.0,<9.0
pytest-cov>=5.0
pytest-socket>=0.7.0   # hard network kill-switch for unit tests (see tests/unit/conftest.py)
