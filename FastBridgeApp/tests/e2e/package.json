{
  "name": "fastbridge-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end and accessibility checks for FastBridge. Non-blocking for now.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.48.0"
  }
}
