version: '3.3'
services:
  web:
    build: .
    command: gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:5001
    volumes:
      - /srv/FastBridge/FastBridgeApp:/app/FastBridgeApp
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
